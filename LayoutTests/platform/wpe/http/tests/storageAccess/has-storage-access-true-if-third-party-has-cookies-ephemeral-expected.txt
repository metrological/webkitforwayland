Tests that document.hasStorageAccess() returns true for a 3rd-party iframe if the 3rd-party has cookies set (ephemeral session).

On success, you will see a series of "PASS" messages, followed by "TEST COMPLETE".


FAIL No storage access when there should have been access. document.cookie == firstPartyCookie=value
PASS successfullyParsed is true
Some tests failed.

TEST COMPLETE

